# Storybook 8.6 déclare un peer dep vite <=6, mais le projet est sur Vite 7.
# npm 7+ bloque par défaut (ERESOLVE). Storybook n'intervient pas dans `vite build`,
# donc on accepte la résolution legacy pour ne pas casser `npm install` (CI / VPS).
legacy-peer-deps=true
